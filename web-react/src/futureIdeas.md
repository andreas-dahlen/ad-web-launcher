hysteresis config? latch to decide if things should lock if i move out of gated area. 

would be really cool with animation trigger when pointer gets inside of "ungated" area again... so it smothly goes to pointer!